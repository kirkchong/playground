// procedures are functions in js
function greetStudent(student) {
    console.log(`helllo ${student.name}`) // interpolated string
}

function timeRemaining(timeElapsed, endTime) {
    return endTime - timeElapsed;
}

